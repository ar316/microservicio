server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/ar316/microservicio
          search-paths: config-data

  application:
    name: config-service